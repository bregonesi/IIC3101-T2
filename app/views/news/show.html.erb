<% provide(:title, truncate(@news.headline, :length => 30)) %>

<div class="row">
	<%= link_to "Edit", edit_news_path(@news), class: "btn btn-primary col-xs-3" %></object>
  <%= link_to "Delete", @news, data: { confirm: "Are you sure?" }, method: :delete, class: "btn btn-danger col-xs-3 col-xs-offset-6" %></object>
  <br>
	<br>
	<br>
	<div class="col-xs-12">
			<font size="5">
				<%= @news.headline %>
			</font>
			<br>
			<br>
			<font size="3">
				<%= simple_format(@news.subhead) %>
			</font>
			<hr>
			<font size="2">
				<%= simple_format(@news.copy) %>
			</font>
			<br>
			<br>
			<font size="1" style="float: right;">
				<%= @news.created_at.strftime("%d-%m-%y %H:%M") %> - <i><%= @news.author.name + " " + @news.author.last_name %></i>
			</font>
			<hr style="height:2px;border:none;color:#333;background-color:#333;">
			<font size="5">
				Comentarios
			</font>

	</div>
</div>

<%= link_to 'Back', news_index_path, class: "btn btn-primary col-xs-3" %>
